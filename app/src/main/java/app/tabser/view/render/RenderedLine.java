package app.tabser.view.render;

/*
import android.graphics.Bitmap;

import app.tabser.view.model.geometry.LineMetrics;

public final class RenderedLine {
    public boolean invalid;
    public Bitmap bitmap;
    public int lineIndex;
    public LineMetrics lineMetrics;

    public RenderedLine(RenderIterator renderResult) {
        invalid = true;
        lineIndex = renderResult.lineOffset;
        calculateLineDimensions(renderResult);
        bitmap = Bitmap.createBitmap((int) lineMetrics.getWidth(), (int) lineMetrics.getHeight(), Bitmap.Config.ARGB_8888);
    }

    public void calculateLineDimensions(RenderIterator renderResult) {

    }
}

 */